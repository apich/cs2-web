# 死亡流程与稳定性修复记录

更新于 2026-09-09。本文分别记录当前对局系统更新、此前装备与探员更新、死亡流程修复，以及仍未定位的整个浏览器关闭问题。修复前版本保留为 Git 标签 `baseline-20260908`，提交 `d6ec42b`。

## 当前发布：20260909T084526Z

本版针对走路回拉和极限跳台抖动，接入固定 60 Hz 移动指令、完整状态确认与重放、逐帧摄像机插值，并将人物碰撞体改为平底碰撞箱，保留台阶和箱沿的支撑。新增 Poly Haven CC0 晴天天空，约 5.2 MiB，已纳入本地缓存。说明与实拍见[移动与天空更新](MOVEMENT-AND-SKY.md)。

**177 项自动回归、819 个资源文件校验、17 项发布工具检查通过。** 本地实际浏览器经过每方向 80±20 毫秒的有序网络延迟，完成 9 静止机器人场景的走路、箱沿静止、短按跳跃和跳箱验收；4 段测量最大位置校正小于 0.5 毫米，走路未测到镜头后拉。7 档开镜命中和快速切刀回归也通过。这些是受控场景检查，不能替代用户设备与全部地图角落的长期游玩。

公网 **9 项双客户端玩法检查 + 3 项新移动协议检查**通过，实测短按跳跃后正常落地，错误生命编号不会推动角色或确认编号。**97 个变更客户端文件、97,469,932 字节**与发布清单逐文件 SHA-256 一致。公网大厅加载 `index-ghSINOVn.js`，鼠标可见，无横向溢出与脚本错误；全地图视觉测试在本地同一构建完成。

上线前真人为 0，保留上一发布 `20260909T072304Z` 及回滚备份，专用 Nginx vhost 未变。完整归档 SHA-256：`9cf74be6fd12fc3b6eeaa0e9d377fa7bfbf4448581cdbbebd04c297641b319c0`。结构化证据见 [movement-20260909.json](validation/movement-20260909.json)。

公网主机另用低优先级独立进程测了 1 个真实 WebSocket 客户端（旧移动协议）加 9 个活动机器人，持续 30 秒。三个 10 秒窗口的 CPU 为单核 22.7%–25.9%，tick p95 为 8.45–9.88 ms，RSS 175–182 MiB，低于 33.33 ms 的单 tick 预算。没有把压测机器人加入在线玩家房间。本次没有证据要求升级服务器；该检查主要约束新碰撞计算的开销，未测试多房间满载，也不将随机机器人路线与旧版数据直接比较为性能提升比例。

## 上一发布：20260909T072304Z

修复开枪瞬间的方向与倍率被后续输入覆盖、开枪后立即切刀漏枪、准星与子弹俯仰不一致，以及 SG 553 镜片被枪身材质遮住的问题。AWP、SSG 08、SCAR-20 的两档镜和 SG 553 举镜动作完成实际浏览器验证；新增最多 200 毫秒的位置回溯，并减少地图矩阵、灯光查找与远处人物动画的重复工作。参数、实拍与性能边界见[开镜与多人性能](SCOPES-AND-PERFORMANCE.md)。

本版 **168 项自动回归通过，817 个资源文件校验通过**。7 种开镜档位全部命中固定目标，并验证快速切刀与 4:3、16:9、21:9 视口。Windows 自动化中的固定瞄准测试隔离了桌面物理鼠标移动，开枪、开镜、切刀仍通过正常鼠标和键盘事件执行；没有修改生产客户端的输入行为。

公网 **9 项双客户端协议检查**通过，包括开枪后立刻退镜、转向、切刀时仍保留原方向和倍率。**95 个变更客户端文件、92,009,497 字节**与打包清单逐文件 SHA-256 一致。公网大厅使用 `index-BXvO7xYM.js`，鼠标可见，无横向溢出或脚本错误。完整地图的 7 档视觉测试在本地同一构建完成，公网另验文件一致性和真实 WSS 协议。

发布前线上真人为 0；上一版 `20260909T044810Z` 和回滚备份保留，专用 Nginx vhost 哈希未变。完整归档 SHA-256 为 `bfc81cf2aadf5d4cb44eb50cf48906a6b2e7930963d63c8f258d6195f1af80c7`。

公网主机隔离压测为 1 房间、1 个实际 WebSocket 客户端加 9 个机器人，真实运行 30 秒。预热后 CPU 约 29%–36% 单核，tick p95 14–17 ms，低于 33.33 ms 的预算；开局存在短时峰值。此次没有证据要求为单间房立即升级服务器，多房间满载及用户实际游玩中的长帧仍需分别观察。此前“整个浏览器关闭”的问题不能仅靠这次开镜与短时负载测试判定已解决。

## 上一发布：20260909T044810Z

这一版加入 G 丢枪 / E 拾取、三档按住投掷、房主调整机器人、MR12 与重复 MR3 加时、100 杀团队死斗，以及换边和胜败结算。探员手臂与外观同步，旧皮肤修正烘焙通道，武器光照跟随地图；机器人瞄准与观战镜头平滑移动。长按 Tab 保持计分板，不再跳走浏览器焦点。

**159 项 Node 回归通过，0 失败**，另有 **17 项部署工具检查**通过。原始地图上的穿透探针验证了 6 组木板、6 组薄金属可穿透，6 组混凝土遮挡；这是网页碰撞与伤害近似实现的验证，不能代表完整 Source 2 穿透算法。

相同构建在本地真实浏览器中验证了 Tab 持续按住、房主调整机器人、三档投掷与取消、丢枪拾取后保留皮肤及 29 发弹药、死亡观战、12 回合换边、加时换边、胜败结算及返回大厅。AWP 两档开镜与曳光、野荷皮肤、换边后的第一人称手臂均有未编辑实拍，见[本次截图](SHOWCASE.md)。比赛过渡由仅监听本机的测试夹具触发，不是公开匹配成绩。

最终客户端连续测试 **204 秒、103 次采样、8 个机器人**，每 7 秒触发死亡、每 21 秒替换机器人，死亡数从 0 到 29。全程保持连接与鼠标控制，未出现脚本错误或 WebGL 中断；纹理峰值 624，JS 堆在 **350–405 MiB** 间，首尾分别为 351 / 371 MiB。它是本次构建的短时间回归，仍不足以确认用户遇到的整个浏览器关闭问题已经消除。

公网 **[cs2.duskrain.cn](https://cs2.duskrain.cn/)** 已激活版本 `20260909T044810Z`。两名普通 WebSocket 客户端端到端完成 **8 项公网检查**：零机器人建房、房主增减机器人与非房主拒绝、三档投掷、取消保留道具、AWP 掉落同步皮肤及弹药、双方正常退出。**90 个变更文件、88,725,339 字节**通过线上逐文件 SHA-256 校验。完整地图的本次浏览器功能测试在本地完成；公网验证覆盖相同发布文件及真实联机协议，另完成大厅浏览器检查，鼠标可见、页面无横向溢出或脚本错误。

当前资源锁包含 **817 个文件 / 365.3 MiB**；基础缓存清单为 **744 个文件 / 190.8 MiB**，可选皮肤与探员手臂按需下载。发布前确认线上没有真人玩家，保留上一发布目录和回滚备份；游戏专用 vhost 哈希未变。公网测试退出后，再检查服务已运行 702 秒，自动重启 0 次、错误日志 0 条，房间和玩家数均回到 0；进程 RSS 117 MiB，服务器可用内存约 3.17 GiB。这不足以推断所有满载场景，目前没有服务器资源不足的证据。

复测脚本：`scripts/playwright-gameplay-qa.js`、`scripts/playwright-match-qa.js`、`scripts/playwright-final-visuals.js`、`scripts/penetration-map-qa.mjs`、`deploy/verify-gameplay-public.mjs` 与 `deploy/verify-gameplay-assets.mjs`。本地原始证据保存在被 Git 忽略的 `artifacts/gameplay-qa/`、`artifacts/deploy/` 和 `output/gameplay-review-full-tests.log`；文档截图另附文件哈希。

## 此前装备与迁移更新：2026-09-09

本次加入 23 支枪械与爪子刀的完整携带清单、37 款武器外观、按阵营选用的四名 CS2 探员，以及护甲、拆弹器和三类道具。默认画质设为最低，亮度可调；武器和可选探员按需加载，实例及特效缓存设有容量限制。死亡流程继续保留战场与击杀信息，死斗自动重生，爆破切换队友观战。

自动检查 **137 项通过，0 失败**。浏览器逐项验证 23 支枪的购买、原模型、皮肤及短点击扣弹，AWP 40° / 10° 两档、SG 553 瞄准、连续购买装备、烟雾和闪光效果通过；四名探员的选择、服务器确认与按阵营保存通过。

最终客户端在反复死亡和替换机器人测试中记录 **152 秒、77 次采样**，死亡数从 2 增至 23，全程保持连接与游戏控制；未观察到脚本错误或 WebGL 中断，JS 堆由 406 MiB 到 384 MiB，纹理峰值 587。这是短时间回归，不能替代所有设备上的长时间游玩。此前本轮的 132 秒短测和下文上一版的 **671 秒、97 次死亡**分别保留为不同构建的记录。

这轮还修复了高帧率下短点击在无物理步的渲染帧中被提前消费的问题，以及快速连续购买撞上服务器限频的问题。更换探员时保留观战位置；死亡中的角色等待重生再替换外观，避免重播倒地动作。

新主地址为 **[https://cs2.duskrain.cn/](https://cs2.duskrain.cn/)**，新主机配置为 **2 vCPU / 4 GiB 内存**。发布版本 **`20260909T032641Z`** 已通过 HTTPS、页面及最终脚本一致性校验；37 款武器外观、3 个道具和 4 名探员共 **44 个模型**全部通过公网 SHA-256 校验。真实双客户端完成 CT/T 共 30 次购买、阵营限制、射击、跳跃、武器皮肤与探员同步后正常退出。独立浏览器完成基础资源下载并进入对局，默认 SAS / 最低画质及 21 项 CT 商店检查通过，页面无脚本错误。

新服务检查记录为 0 重启、0 OOM、0 错误日志，可用内存约 3.19 GiB。首次基础资源下载约 184 MiB；并行下载时曾测到一次 1543 ms WSS 往返，下载结束后浏览器显示约 42 ms；随后 7 次独立 WSS 采样为 **38.62–52.72 ms，中位数 39.98 ms**。两者测试负载不同，不能把下载拥塞归因于游戏逻辑或 CPU。

旧站真人离开后，再次确认 0 人 / 0 房间才启用 `/dust2/` 到新站的重定向，保留路径与房间参数。仅替换游戏专用 Nginx include，首页与既有 vhost 的 SHA 保持不变；旧游戏进程保留为回退，不再承接旧入口的新连接。

增加服务器内存不等于解决客户端浏览器退出。用户报告的是**整个浏览器直接关闭**，该现象的根因仍未确认；目前没有证据可以据此归因于服务器性能，也不宣称已经消除。

## 玩家能看到的变化

- 死亡后保留战场，显示击杀者、武器、爆头信息与重生倒计时。死斗收到服务器重生状态后自动恢复操作，无需再次点「继续游戏」。
- 爆破死亡后先保留短暂死亡视角，再观察存活队友；默认左键、右键切换队友。Esc 仍可随时打开菜单并恢复鼠标，观战时也能从菜单返回。
- 网络断开显示断开代码及已知原因。若 WebGL 渲染上下文中断，显示恢复界面；浏览器恢复后可继续，或利用已缓存资源重新载入。
- 大厅和对局菜单提供「导出运行诊断」。记录只保存在本机，最多 60 条资源采样与 40 条事件，保留上一轮页面记录；不上传玩家信息、账号或凭据。

死亡与观战流程参考 [Valve Spectator UI](https://blog.counter-strike.net/2012/08/4839/) 与 [Steam 死亡镜头说明](https://help.steampowered.com/en/faqs/view/0E82-09BC-324C-CB12)，不是对官方闭源界面的逐像素复制。

## 上一版已确认并修复的缺陷

| 问题 | 修复 |
| --- | --- |
| 死亡主动退出 Pointer Lock，触发通用暂停菜单 | 把死亡状态与玩家主动打开菜单分开；死亡、重生只清空旧输入，不自动解锁鼠标 |
| 移除角色时遗留克隆骨骼纹理、脚底标记及动画缓存 | 释放实例独占资源，包括已从当前武器树移除的缓存模型；保留共享材质、几何与皮肤图像 |
| 暂停 AudioContext 时取消声音，等待延后的 `onended` 才断开节点 | 取消时立即幂等释放；自然播放完成走同一清理路径 |
| 特定畸形 join/buy 字段触发字符串强制转换异常，使服务器进程退出 | 在协议入口检查字段类型；拒绝坏消息后其他连接仍可正常工作 |
| 静态资源两次 stat 之间被替换，可能产生未处理拒绝 | 复用第一次文件查找的元数据；流读取失败终止该响应 |
| 拒绝过多连接时，关闭握手期间的非法帧可能没有错误处理器 | 在容量检查之前安装 socket 错误处理器 |

用户进一步明确，原始现象是整个浏览器关闭，而非仅返回大厅。这几项是可验证的缺陷。普通网页不会发送上述畸形消息；同一角色死亡重生也会复用模型。因此不能把它们直接当成用户全部闪退现象的唯一原因。

## 上一版验证：2026-09-08

- 自动化回归：**114 项通过，0 失败**。包含输入、跳跃、购买、皮肤、缓存、联机、死亡状态、资源生命周期与服务器容错。
- 浏览器功能检查：死亡时鼠标仍锁定且没有暂停菜单；重生立即可移动；Esc 恢复菜单和鼠标；爆破可观察并切换存活队友。
- 生命周期验证：重复移除 80 组角色，释放全部实例资源；实际 GLB 结构重复克隆/清理 50 次，SWAT 600/600、Hoodie 500/500、AWP 与手臂各 100/100 骨骼纹理释放，共享资源保留。
- 服务器隔离压力：4 房间、40 名角色，其中 32 个机器人，真实 WebSocket 运行 90 秒；RSS 约 131–132 MiB。之后执行 600 秒游戏时间的加速模拟，实际耗时 176.7 秒；这部分不是 10 分钟真实联网运行。事件和路径保持有界，全部客户端离开后房间及连接归零。
- 修复后服务器短测：40 个连接均正常保持，0 提前断开、0 传输或协议错误；退出后 GC 堆内存约 16 MiB。另验证慢连接写队列拥塞只断开该连接，其他玩家仍正常。

上一版独立无界面 Chrome 以 2560×1273 连续运行 **671 秒**，8 个机器人，每 7 秒触发一次真实服务器死亡、每 21 秒替换全部机器人，共记录 **97 次死亡**。335 个采样全部保持连接；页面外监听未记录 crash 或 close，诊断未记录脚本错误或 WebGL 中断。最后 120 秒纹理保持 **612**，几何资源在 1928–1936 间随瞬时效果变化，JS 堆为 245–332 MiB，未出现随替换次数持续增长。此测试不是所有设备、全部浏览器与无限时长的保证，也不包含本次新增探员和道具的完整覆盖。

上一版实际 WebGL 丢失/恢复测试通过：显示恢复界面、重新生成武器环境反射，地图与武器恢复渲染后能重新锁定鼠标继续；没有脚本异常。旧站 `/dust2/` 当时部署为 `20260908T142657Z`，HTTPS、静态资源和双客户端 WSS 的移动、购买、皮肤与跳跃验证通过，测得一次往返 63 ms。2395 个发布文件经过校验，站点原首页哈希保持不变。该结果不代表新域名已完成迁移验收。

## 上一版服务器与本机资源记录

2026-09-08 检查时，旧站游戏服务没有异常重启、内存限制触发或内核 OOM 记录。旧主机为 2 CPU / 2 GiB，游戏进程约 138 MiB；单房间 8 机器人实测 60 秒，RSS 保持 139 MiB，收到 942 次快照，结束后房间归零。**这些证据不指向当时的服务器性能不足**，也不代表已经验证了所有公网满载场景。

本机检查发现过 C 盘剩余空间为 0、16 GiB 内存仅约 0.93 GiB 空闲，以及 8 GiB 显存使用约 7.1 GiB。关闭本任务遗留的两个临时浏览器会话后，C 盘恢复约 6.4 GiB 可用空间。此操作没有清除正常浏览器的用户数据。

上一版完整地图 510 张纹理以 RGBA8 加 mipmap 估算约 1.22 GiB；当时默认武器与手臂再约 0.21 GiB，另外还有几何、阴影、帧缓冲和驱动开销。这是旧资源尺寸估算，并非当前版本单页实测显存。多个高负载网页会共同占用本机内存与显存；当时保留原贴图和画质，本次则按用户要求将初始画质设为最低，并保留调节入口。

一次有界面测试在约 7 分半后被空白页打断：之前采样无脚本错误或 WebGL 中断，自动化工具在页面关闭或崩溃后都可能重建空白页，现有记录无法区分两者。因此该次不计为完整通过，后续独立会话增加页面外的 crash、close 和导航监听复测。磁盘和显存压力与中断同时被观察到，但尚不能证明因果关系。

## 复测方式

```sh
npm test
node scripts/server-audit.mjs --seconds=90 --soak=600 --output=output/server-audit/new-run.json
```

本地死亡/观战浏览器验收使用 `scripts/qa-stability-server.mjs` 与 `scripts/playwright-stability-qa.js`，显卡恢复验收脚本为 `scripts/playwright-graphics-recovery-qa.js`。前者只监听回环地址，通过真实 GameRoom 状态制造死亡与机器人替换；控制端口不会打进线上发布包。测试截图不是公开匹配战绩。


## 2026-09-09 房间 / 换弹 / C4 更新

已发布 `20260909T104043Z`，从 `20260909T084526Z` 的运行基线升级。完整包 SHA-256：`c56b7175606d862617da09a6747e53068ca29a2386d53a40c9c197e89089d836`。服务器仅切换专用游戏发布目录并重启游戏服务；专用 Nginx 配置 SHA-256 保持 `2a80a8db07363ffff6019b6dfdb46d1308fc805f8a409c88730089d9a11d2607`。

发布前读取到 1 位真人、10 个角色，p95 模拟耗时 9.63 ms，33.33 ms 为单帧预算；进程 RSS 154 MiB、整机可用内存约 3.2 GiB。该样本不支持“目前必须升级服务器”的判断，也不替代多房间容量测试。用户明确选择立即更新并重新加入后执行服务切换。

本次新增 5 对 5 固定席位、按动作事件提交的装弹、原版 C4 及音乐盒。全量 186 项测试通过，最后的 9 项专项回归包含无枪安放后自动切刀及掉落弹药的攻击恢复限制。资源锁含 972 个文件；新增可选音乐不预加载，只保存至多三个压缩片段。具体操作和截图见 [房间、换弹与 C4](ROOM-RELOAD-C4.md)。

上线后通过 5 项房间、9 项对战、3 项移动公网检查，以及 251 个变更客户端文件（116,752,455 字节）的完整 SHA-256 校验。公开网址实际浏览器选位和人机增减也已通过；[公网记录](validation/room-public-20260909.json)保留结果。首次大文件验证曾因 90 秒下载超时中止；重试全部校验通过，验证工具现在保存检查点并允许 180 秒单文件传输。
