# Windows 便携包

把完整 ZIP 发给朋友，朋友解压整个文件夹后双击启动。不需要安装 Node、npm 或 Steam；使用电脑已有的 Edge / Chrome。目标平台为 Windows 10/11 x64。

当前触屏与人机协作更新包：`DustII-Windows-x64-20260913-035203.zip`，约 352.5 MiB。包含本次人机分工、经济、补枪和小幅枪法调整，以及触屏界面。ZIP 与 2442 个文件校验通过，详见[更新验收](MOBILE-BOT-BALANCE.md#发布验收)。离线人机逻辑随包发布，旧包不会自动替换本地服务器。

| 入口 | 用途 |
| --- | --- |
| 开始游戏-在线联机.cmd | 本机读取所有资源，通过 `wss://cs2.duskrain.cn/ws` 连接公网对局 |
| 开始游戏-离线机器人.cmd | 在本机启动权威游戏服务器，可断网与机器人对战 |
| 校验文件.cmd | 按包内清单校验全部文件的大小与 SHA-256 |

在线联机时，一人创建房间，朋友用自己的本地包输入相同房间码。双方合计 10 个席位；房主可以在空位添加人机。邀请链接仍使用公网游戏地址，供网页玩家加入。本地包用户用房间码加入可直接使用本机资源。

离线房间仅属于当前电脑，不能用离线房间码邀请异地朋友。离线模式点击邀请按钮会说明应改用在线入口。

## 包含内容

- 当前资源锁清单中的 1057 项资源，包括地图、枪械、探员、手臂、皮肤、声音及可选音乐盒。
- 编译后的客户端、机器人服务器、纯 JavaScript 生产依赖。
- Node.js v22.23.2 的 Windows x64 运行程序和许可；来自官方 ZIP，并与官方 SHA-256 清单核对。
- 中文使用说明、素材来源、第三方许可和逐文件校验清单。

首次加载仍需要读取资源、校验并建立 3D 场景，进度条表示本机加载。游戏设置保存在当前浏览器；便携包不携带制作者的昵称或浏览器设置。在线和离线入口使用不同的本机端口，因此两者设置分别保存。

游戏期间保留启动窗口，结束后关闭浏览器和启动窗口即可。文件夹可以整体移动或复制；不需要管理员权限。浏览器仍承担渲染，本包不承诺提升帧率。

## 构建与边界

```sh
npm run build
node scripts/build-asset-lock.mjs
python deploy/package-portable.py
```

输出位于 `artifacts/portable/`，旁边生成 ZIP 的 SHA-256 文本。运行程序只监听 `127.0.0.1`；普通静态服务仅能读取包内 `dist`。启动时端口被同一份包占用会复用已有入口，被无关程序占用则换端口，不关闭其他程序。

压缩包通过允许列表组装，不包含部署脚本、服务器凭据、Git 元数据或个人浏览器资料。在线入口固定连接既有服务器；不会重启或更新公网服务。本包不自动升级，后续不兼容版本需要重新获取客户端包。

## 验证

2026-09-09：189 项自动测试通过，包含本地 HTTP 路径边界、音频 Range、重复启动、无关端口占用和客户端连接目标。

实际 Chrome 验证了两个独立客户端加入同一公网房间，双方看见两名真人；资源请求均为本机地址，邀请为公网地址。离线模式屏蔽非本机 HTTP 请求后加载地图、武器并运行 3 名机器人。记录见 `docs/validation/portable-20260909.json`。

2026-09-11 更新：加入回合击杀卡、台阶碰撞修复、切枪声音、右键重刀、完整五类道具和固定蹲姿下包/拆包。新版共享物理与服务器同步，旧本地包应换成新包。验证记录见 `COMBAT-FEEDBACK.md`。

当前包：`DustII-Windows-x64-20260911-034243.zip`，355,467,074 字节；SHA-256：`3a33a6de940815ab1746fbacfd1716659ff04762d9858a30f638b6562b68d997`。包含 1024 项锁定资源，2401 个清单文件，基于源码提交 `848ac76` 构建。

2026-09-13 更新：加入联机房间列表、16:9 / 4:3 与拉伸选项、M9 / 蝴蝶刀、三款按需音乐盒、设置备份、战术人机、跳投时序、退枪及空槽自动拾取。完整包内已包含新增资源，武器库中的下载操作从本机读取。

当前包：`DustII-Windows-x64-20260912-180750.zip`（369,608,233 字节；SHA-256：`2ce50f3ade52c2d734ff65e1c7b0428a97d6c35e9fc3a33e8e80aa82a250f5cd`）。2436 个文件的逐项哈希与 ZIP CRC 校验通过。实际验证了 3 名人机的离线运行，以及从联机大厅加入公网房间并看到两个真人连接，资源 HTTP 请求均来自本机。详见 `validation/extras-20260913.json`。
